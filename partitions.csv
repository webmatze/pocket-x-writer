# Partition table matching the Xteink X4 Pro EXACTLY, read from this device's
# own flash backup. Do not "improve" these numbers.
#
# This file is here so PlatformIO sizes the app against the real 0x7e0000 slot
# instead of its 6.25 MB default -- and so an accidental `pio run -t upload`
# cannot write a partition table that would destroy the other OTA slot.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x5000
otadata,    data, ota,      0xe000,   0x2000
app0,       app,  ota_0,    0x10000,  0x7e0000
app1,       app,  ota_1,    0x7f0000, 0x7e0000
spiffs,     data, spiffs,   0xfd0000, 0x14000
coredump,   data, coredump, 0xfe4000, 0x1c000
